program factorial

	# d e c l a r a t i o n s #
	declare x;
	declare i, fact;
	# m a i n #
	{
		input (x);
		fact := 1;
		i := 1;
		fact := fact*(x+10)/(i-2);
		i := i+1;
		print(fact);
	}.