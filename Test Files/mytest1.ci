program Test

	declare x, y;

	# main #
	{
		if (x>=y)
		{
			print(x) ;
		}
		else
		{
			print(y);
		};
		if (x>=y)
		{
			if (x>=y)
			{
				print(x) ;
			}
			else
			{
				print(y);
			};
			
			print(x) ;
			
		}
		else
		{	switchcase

				case([xge>0 or [gwb<>mge]])
				{
					input(xge);
					forcase
						case(gex>m)
						{
							y := 4+10+gge1;
							print(i);
						}
						case(i<=4)
						{
							while(i>bge)
							{
								input(x);
								print(x);
								b:=b+1;
							};
	
						}
						default
						{
							if(a>=15)
							{
								print(ahgeh);
						
							}
							else
							{
								print(hrem);
		
							};
	
						};

				}

				default
				{

					t:=34 ;
					u:=5+t-45;
					x:=t*u;

				};
			
			print(y);
		
		};
		
	}.
	