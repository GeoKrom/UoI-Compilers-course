program test5

declare x,y,z;

function Test( in x, in y)


{
	if(x>y)
	{
	return(x) ;
	}
	else
	{
	
	return(y) ;
	
	};


}

function Test2( in x, in y)
{
return(x+y);
}

#main#
{

	input(x);
	input(y);
	z:=Test2(in (Test(in x+1, in y*2)), in (Test(in x, in y)));
	print(z);
	if([[x>1 and y<2] or [z=18]])
	{
		print(z+1);
	};	
	

}.