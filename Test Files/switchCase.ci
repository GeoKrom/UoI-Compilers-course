program checkSwitchCase
declare x,y;
#main#
{
	input(x);
	switchcase
		case(y<=0)
		{
			return(y);
			y:=x*2;
		}
		case(x>0)
		{
			print(y*3);
			y:=x*2;
		}
	default return(x*2);;
	
	return(x);
}.