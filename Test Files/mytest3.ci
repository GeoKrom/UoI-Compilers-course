program TEST

	declare x,y,u;
	
	declare a ,b,c ;

	#main#
	{
		input(a);
		input(b);
		input(c);
		input(x);
		input(y);
		
		while(a>b)
		{
			input(x);
			print(x);
			b:=b+1;
		};
		
		forcase
		
			case(x>=0 or b<= ( c+9))
			{
				x:=x+1;
				print(x);
			
			}
			
			case(x=1)
			{
				input(a);
				a:=a+1;
				print(a);
			
			}
			
			case(x>5)
			{
			
				y:=a+b+c;
				print(y);
			}
			
			default
			{
				
				if(a>dgg123g)
				{
					print(a);
					
				};
			};
		
		print(x);
	}.